import React from "react";
import styled from "styled-components/macro";

interface OwnProps {
    
}

const GameButton = (props: OwnProps) => {
    return(
        <Button>

        </Button>
    )
}

const Button = styled.button`

`